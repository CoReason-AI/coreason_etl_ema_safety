[build-system]
requires = ["hatchling", "hatch-vcs"]
build-backend = "hatchling.build"

[project]
name = "coreason_etl_ema_safety"
dynamic = ["version"]
description = "ETL process for integrating European Medicines Agency (EMA) safety data"
readme = "README.md"
requires-python = ">=3.14"
authors = [
    { name = "Akshaya M", email = "akshaya.movvar@coreason.ai" },
]
dependencies = [
    "dlt>=1.23.0",
    "fastexcel>=0.19.0",
    "loguru>=0.7.2",
    "polars>=1.39.0",
    "requests>=2.32.5",
    "dbt-core>=1.9.0",
    "dbt-postgres>=1.9.0",
]
license = { file = "LICENSE" }
classifiers = [
    "License :: Other/Proprietary License",
    "Operating System :: OS Independent",
]

[dependency-groups]
dev = [
    "pytest>=9.0.2",
    "ruff>=0.14.14",
    "pre-commit>=4.5.1",
    "pytest-cov>=7.0.0",
    "zensical",
    "mypy>=1.19.1",
    "pytest-asyncio",
    "pytest-benchmark",
    "deptry",
    "deepdiff",
    "hypothesis",
    "syrupy",
    "mkdocstrings-python",
    "types-pyyaml>=6.0.12.20250915",
    "tzdata>=2025.3",
    "pytest-xdist>=3.6.1",
    "types-requests>=2.32.4.20260107",
    "responses>=0.26.0",
    "pydantic>=2.12.5",
]

[tool.hatch.build.targets.wheel]
packages = ["src/coreason_etl_ema_safety"]

[tool.hatch.version]
source = "vcs"

[project.urls]
Homepage = "https://github.com/CoReason-AI/coreason_etl_ema_safety"
Repository = "https://github.com/CoReason-AI/coreason_etl_ema_safety"
Documentation = "https://github.com/CoReason-AI/coreason_etl_ema_safety"

[tool.ruff]
line-length = 120
target-version = "py314"

[tool.ruff.lint]
select = ["E", "F", "B", "I", "UP", "SIM", "RUF", "ARG", "C4", "PT", "TCH", "FA", "PIE", "RET", "PERF", "FURB", "LOG", "N", "A", "S"]
ignore = ["S101", "TC001", "TC002", "TC003", "UP037"]

[tool.mypy]
python_version = "3.14"
strict = true
disallow_untyped_defs = true
warn_unused_ignores = true
plugins = ["pydantic.mypy"]

[[tool.mypy.overrides]]
module = [
    "responses",
    "hypothesis",
]
ignore_missing_imports = true

[[tool.mypy.overrides]]
module = [
    "tests.test_transform",
    "tests.test_extract",
]
disallow_untyped_decorators = false

[[tool.mypy.overrides]]
module = [
    "coreason_etl_ema_safety.pipeline",
]
warn_unused_ignores = false
disallow_untyped_decorators = false

[tool.pytest.ini_options]
addopts = "-n auto --cov=src --cov-report=term-missing --cov-report=html --cov-fail-under=100"
testpaths = ["tests"]
asyncio_mode = "auto"

[tool.coverage.run]
omit = ["tests/*"]

[tool.deptry.per_rule_ignores]
DEP002 = ["dlt", "fastexcel", "polars", "requests", "loguru", "dbt-core", "dbt-postgres"]

[tool.uv]
override-dependencies = ["mashumaro[msgpack]>=3.15"]
